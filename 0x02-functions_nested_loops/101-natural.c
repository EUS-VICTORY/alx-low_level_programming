#include "main.h"
#include <stdio.h>
/**
 * -natural.c - prints all natural numbers that are multiples of 3 and 5
 *  and sum them 
 *  Return: Always 0 (Success)
 */

int main(void)
{
int x < 1024, y, sum = 0
for (y = 0, y < x, y++)
{if ((y % 3 == 0) || (y % 5 ==0))
{
sum = sum + y
}
}
printf("%d\n", sum);
return (0);
}
