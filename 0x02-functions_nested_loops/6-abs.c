#include "main.h"
/**
 * _abs(int) - computes absolute value of an integer
 * @n: the parameter to input
 * Return:Always 0
 */
int _abs(int)
{
n = abs(n)
if (n < 0)
{
return (n*(-1))
}
else if (n == 0)
{
return (0)
}
else
{
return (n)
}
}	
