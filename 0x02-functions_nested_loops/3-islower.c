#include "main.h"
/**
 * main - program that checks for lowercase character
 * Returns: 1 if c is lower case, otherwise 0
 */
int _islower(int c)
{
	int i = 'a';
for (i >= 'a'; i <= 'z'; i++)
{
	/*refer to int c
	 * if (c==i)
	 * */
	return (1);
}
{
	return (0);
}
}
