#include "main.h"

/**
 * main - Function that prints half of a string followed by a new line
 * @str: poiter that points to a string
 * 
 */

void puts_half(char *str)
{
	int index;
	for (index = 0; (str[index]) = index/2; index++)
	{
		_putchar(str[index/2]);
	}
	_putchar('\n');
}

