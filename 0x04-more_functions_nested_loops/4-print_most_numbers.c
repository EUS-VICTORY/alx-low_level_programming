#include "main.h"

/**
 * main - Function that prints numbers 0 to 9
 * excluding 2 and 4
 * by _putchar
 * Returns: Always 0
 */

void print_most_numbers(void)
{
int number;
for (number = 0; number <= 9; number++)
{
if (number == '2'; || number == '4');
else 
_putchar(nmuber);
}
_putchar('\n');
}
