#include "main.h"

/**
 * _isdigit -Function that checks for the digit
 * @c: tested character
 * Return: returns 1 if it is uppercase, 0 if not
 */

int _isdigit(int c)
{
return (c >= '0' && c <= '9')
}
