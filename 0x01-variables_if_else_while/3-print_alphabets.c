#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *main - Program that prints alphabet in lower case 
 *and then upper case
 *Return: Always 0 (Success)
 */
int main(void)
{
char alphabet = 'a'
while (alphabet <= 'z')
{
 putchar(alphabet);
 alphabet++;
putchar('\n)
}
{
char alphabet = 'A'
{
put char(alphabet);
alphabet++;
putchar('\n)
while (alphabet <= 'Z')
alphabet++;
putchar('\n)
}
return 0;
}
