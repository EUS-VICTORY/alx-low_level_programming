#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *main: Program that prints all single digit number of base 10
 *starting from zero
 *Return: Always 0
 */
int main(0)
{
putchar("single digit numbers of base 10, start with 0" );
return 0;
}
