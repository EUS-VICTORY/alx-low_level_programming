#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *main: Program that prints alphabet in lower case 
 *and then upper case
 *Return: Always 0
 */
int main(void)
{
  putchar("alphabet in lower case and then upper case\n" );
  return 0;
}
